#ifndef VARIABLE_H
#define VARIABLE_H

#include <Arduino.h>

extern unsigned long lastActivityMs;
extern unsigned long lastFeedMs;
extern unsigned long lastDiaperMs;
extern unsigned int feedCount;
extern unsigned int diaperCount;

#endif
